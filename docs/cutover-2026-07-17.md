# PGPZ sites production cutover record — 2026-07-17

## Scope and status

This record covers the source-topology cutover of the existing PGPZ Coalition
and PGPZ Community Amplify applications to `paulbrigner/pgpz-sites`. It does
not move or recreate production domains, tables, IAM roles, SES identities,
secrets, or member data. Disposable preview-only tables, roles, and synthetic
records are recorded separately below.

Current status: **Coalition production deployment and active-member smoke
passed; the Coalition soak is in progress**. Explicit Amplify job `41`
succeeded at `2026-07-17T12:41:28.186Z`; production auto-build remains off.
The live administrator-only UI check remains an explicitly tracked soak item,
not a confirmed regression. Community remains on its original repository and
is blocked from cutover until a healthy review after `2026-07-20 09:00`
America/New_York.

## Immutable source and deployment baseline

| Application | Amplify app | Repository | Branch | Auto-build | Last successful job | Deployed commit |
| --- | --- | --- | --- | --- | --- | --- |
| Coalition | `d1ve1xrza71r7u` | `paulbrigner/pgpz-coalition` | `main` (`PRODUCTION`) | enabled | `40` | `ffffec98878729658f96ddba6624c73b316279f6` |
| Community | `d2xb9ethk5a24j` | `paulbrigner/pgpz-community` | `main` (`PRODUCTION`) | enabled | `117` | `d6a1d1876dbdd5f0959d43ea3c8a19ebf70334ac` |

This table is the immutable pre-cutover baseline. The application-level
Amplify build spec was unset for both apps, so each used the `amplify.yml` in
its original repository. Pull-request preview deployment was disabled on both
production branches.

Existing domain associations must not change:

- Coalition: `coalition.pgpz.org` -> `main`.
- Community: `community.pgpz.org` and `community.pgpforcrypto.org` -> `main`.

The frozen monorepo candidate is commit
`b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea`. GitHub Actions run
[`29575014790`](https://github.com/paulbrigner/pgpz-sites/actions/runs/29575014790)
passed clean installation, history and parity checks, dependency-boundary
checks, typechecking, 313 tests, linting, environment materialization, and both
application builds with non-production environment values.

## Environment boundary audit

The inventories below contain key names and digests only. They do not disclose
environment values. A value-preservation digest removes
`AMPLIFY_MONOREPO_APP_ROOT`, sorts the remaining entries by key using
`localeCompare`, serializes `Object.fromEntries` as JSON, and hashes that JSON
with SHA-256.

Coalition's pre-cutover inventory contained 18 keys:

`BETTER_AUTH_SECRET`, `BETTER_AUTH_TRUSTED_ORIGINS`, `BETTER_AUTH_URL`,
`EMAIL_FROM`, `EMAIL_SERVER`, `EMAIL_SERVER_PASSWORD`, `EMAIL_SERVER_PORT`,
`EMAIL_SERVER_SECURE`, `EMAIL_SERVER_USER`, `NEXTAUTH_SECRET`,
`NEXTAUTH_TABLE`, `NEXTAUTH_URL`, `NEXT_PUBLIC_SITE_URL`,
`PGPZ_AWS_ACCESS_KEY_ID`, `PGPZ_AWS_SECRET_ACCESS_KEY`,
`POLICY_UPDATE_UPLOAD_BUCKET`, `POLICY_UPDATE_UPLOAD_PREFIX`, and `REGION_AWS`.

Its value-preservation digest was and remains
`e25dfff1a99166b1c564fc0acbdcb703ef59583105f69d8788316bcd7a80cd49`.
The production reconnect added only
`AMPLIFY_MONOREPO_APP_ROOT=apps/coalition`, bringing the current key count to
19; all original values remained byte-for-byte stable under the digest above.
No Community-only key was introduced.

Community's untouched production inventory contains 33 keys:

`BETTER_AUTH_SECRET`, `BETTER_AUTH_TRUSTED_ORIGINS`, `BETTER_AUTH_URL`,
`EMAIL_FROM`, `EMAIL_SERVER`, `EMAIL_SERVER_PASSWORD`, `EMAIL_SERVER_PORT`,
`EMAIL_SERVER_SECURE`, `EMAIL_SERVER_USER`,
`MEMBERSHIP_PROOF_RETENTION_POLICY`, `NEXTAUTH_SECRET`, `NEXTAUTH_TABLE`,
`NEXTAUTH_URL`, `NEXT_PUBLIC_SITE_URL`, `PGPZ_AWS_ACCESS_KEY_ID`,
`PGPZ_AWS_SECRET_ACCESS_KEY`, `POLICY_UPDATE_GENERATION_BASE_URL`,
`POLICY_UPDATE_GENERATION_MAX_TOKENS`, `POLICY_UPDATE_GENERATION_MODEL`,
`POLICY_UPDATE_GENERATION_TIMEOUT_MS`, `POLICY_UPDATE_UPLOAD_BUCKET`,
`POLICY_UPDATE_UPLOAD_PREFIX`, `REGION_AWS`,
`SOCIAL_PROOF_AUTOVERIFY_SECRET`, `VENICE_API_KEY`, `X_API_BASE_URL`,
`X_API_TIMEOUT_MS`, `X_BEARER_TOKEN`, `X_PROOF_AUTOVERIFY_BATCH_SIZE`,
`X_PROOF_AUTOVERIFY_GROUP_SIZE`, `X_PROOF_AUTOVERIFY_MAX_ATTEMPTS`,
`X_PROOF_AUTOVERIFY_WINDOW_MINUTES`, and `X_PROOF_CHALLENGE_TTL_MINUTES`.

Its untouched value-preservation digest is
`49d9ee7d1e02db41ebce43a5a318bbe2a73d274ca70b88c84a5f4358331d1c50`.
The following five unused legacy generation keys remain stored on the
production Amplify app but are intentionally omitted from monorepo build
serialization:

- `POLICY_UPDATE_GENERATION_BASE_URL`
- `POLICY_UPDATE_GENERATION_MAX_TOKENS`
- `POLICY_UPDATE_GENERATION_MODEL`
- `POLICY_UPDATE_GENERATION_TIMEOUT_MS`
- `VENICE_API_KEY`

Those values must remain stored throughout the rollback observation window.
Both inventories include non-default Better Auth URL, secret, and
trusted-origin configuration. Email tracking retains fingerprint continuity
through the existing `NEXTAUTH_SECRET` compatibility value. No environment
value was copied between apps.

## DynamoDB schema gate

Both existing tables are `ACTIVE`, use on-demand billing, and passed the
required read-only schema checks:

| Application | Table | Base key | GSI | TTL |
| --- | --- | --- | --- | --- |
| Coalition | `PGPZCoalitionNextAuth` | `pk` (HASH), `sk` (RANGE) | `GSI1`: `GSI1PK` (HASH), `GSI1SK` (RANGE), `ALL`, `ACTIVE` | `expires`, `ENABLED` |
| Community | `PGPZCommunityNextAuth` | `pk` (HASH), `sk` (RANGE) | `GSI1`: `GSI1PK` (HASH), `GSI1SK` (RANGE), `ALL`, `ACTIVE` | `expires`, `ENABLED` |

No production schema or data mutation was performed for this gate.

## Gate log

| Gate | Status | Evidence |
| --- | --- | --- |
| Clean Node 22 root install | passed | Hosted CI run `29575014790` |
| Imported history | passed | Hosted CI run `29575014790` |
| Typecheck, tests, and lint | passed | Hosted CI run `29575014790` |
| Both application builds with non-production environment | passed | Hosted CI run `29575014790` |
| Deliberate CI failure proof | passed | Closed PR `#1`; run `29576460173` failed at the intentional Vitest assertion and skipped dependent builds |
| Two previews per app | passed | Frozen commit `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea`; Coalition jobs `1` and `2`; Community jobs `1` and `2` |
| Authenticated and lifecycle preview matrix | passed | Better Auth callback/session/sign-out, admin/member authorization, profile changes, email flows, and app-specific workflows recorded below |
| Environment-key audit | passed | Counts, names, and value-preservation digests above; five Community keys deliberately retained but omitted from serialization |
| DynamoDB key/GSI/TTL audit | passed | Read-only AWS checks above |
| Timed rollback rehearsal | passed | Both tagged legacy commits built, deployed, and passed route, asset, session, NextAuth-absence, and magic-link smoke by `2026-07-17T11:46:35Z` (7m28s) |
| Protected monorepo `main` | passed | Strict `Checks`, `Build community`, and `Build coalition`; PR required; force-push/deletion disabled |

Both original repository `main` branches are also protected against direct and
force pushes and deletion, with administrator enforcement and PR-only changes.
The exact production commits are published in each source repository as the
annotated tag `monorepo-cutover-baseline-2026-07-17`.

## Isolated preview targets

The frozen candidate branch `cutover-preview-20260717` points at monorepo
commit `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea`. It is attached to two
temporary, basic-auth-protected `WEB_COMPUTE` applications with auto-build and
pull-request previews disabled:

| Application | Preview app | URL | Isolated table |
| --- | --- | --- | --- |
| Coalition | `d1pjclkrx6w70n` | `https://cutover-preview-20260717.d1pjclkrx6w70n.amplifyapp.com` | `PGPZCoalitionCutoverPreview20260717` |
| Community | `d1fapo5s6zyija` | `https://cutover-preview-20260717.d1fapo5s6zyija.amplifyapp.com` | `PGPZCommunityCutoverPreview20260717` |

The preview tables reproduce the production `pk`/`sk`, `GSI1`, on-demand
billing, and `expires` TTL contract but contain no production member data. Each
preview was seeded with two synthetic active administrators, a synthetic active
non-administrator, and additional synthetic users for signup, profile-change,
invitation, approval, welcome-email, and synchronization lifecycle checks. The
preview apps use unique auth and tracking secrets, copied SMTP transport
settings, and temporary Amplify compute roles limited to their preview
DynamoDB resources. Coalition can query and update only the isolated Community
preview table for its membership-sync path. Neither app receives production
PGPZ AWS access keys or production upload-bucket configuration.

### Consecutive deployment evidence

| Application | Job | Commit | Start | Complete | Expanded smoke |
| --- | --- | --- | --- | --- | --- |
| Coalition | `1` | `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` | `2026-07-17T11:30:25.714Z` | `2026-07-17T11:38:28.780Z` | passed |
| Coalition | `2` | `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` | `2026-07-17T11:47:19.034Z` | `2026-07-17T11:52:22.389Z` | 11/11 passed |
| Community | `1` | `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` | `2026-07-17T11:30:25.099Z` | `2026-07-17T11:36:56.679Z` | passed |
| Community | `2` | `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` | `2026-07-17T11:47:19.156Z` | `2026-07-17T11:52:32.786Z` | 11/11 passed |

Each expanded smoke checked `/`, `/signin`, `/updates`, `/terms`, `/privacy`,
the app-specific `/zec-shelf` or `/groups` route,
`/api/auth/session/app`, `/api/better-auth/get-session`, absence of the legacy
NextAuth provider route, the logo, and a representative public PDF. The
server-side PDF worker was present in both candidate build artifacts at
`.next/server/chunks/pdf.worker.mjs` with a size of **1,099,167 bytes**.

One Better Auth magic-link request per app returned HTTP 200 in the first pass.
The isolated tables contained the expected
`BETTER_AUTH#better_auth_verifications`, durable `BETTER_AUTH#RATE_LIMIT`, and
`EMAIL_LOG` record types alongside synthetic `USER` records. Fresh second-pass
links were then consumed in real browser sessions for the authenticated matrix
below; no token or message body is recorded here.

### Authenticated, authorization, and lifecycle matrix

| Area | Community result | Coalition result |
| --- | --- | --- |
| Better Auth lifecycle | Fresh magic link consumed; app session persisted across navigation; sign-out cleared the session | Fresh magic link consumed; app session persisted across navigation; sign-out cleared the session |
| Administrator surface | Admin console, user rows/details, administrator indicator and toggle controls rendered; self-removal remained disabled | Same; invitation template was collapsed by default |
| Active-member authorization | Synthetic active non-admin reached member content and was denied administrator access | Synthetic active non-admin reached member content and was denied administrator access |
| View as member | Entry point was available from the page template; member banner appeared, admin navigation was hidden, member content remained usable, and exit restored admin mode | Same |
| Profile email change | Verification was sent and consumed; application and Better Auth identity/index remained consistent; sign-in and sign-out worked with the changed address | Same |
| Public/member navigation | Canonical Community name, legal links, member navigation, profile, updates, and static assets were correct | Canonical Coalition name, legal links, member navigation, profile, updates, and static assets were correct |
| App-specific route | ZEC Shelf rendered its resource library and no assigned ordinal numbers were displayed | Policy Groups rendered for the active member |

Community-specific lifecycle checks also passed:

- Referral signup applied the invitation attribution, sent the verification
  message, and exposed the manual-approval alternative without invoking the
  external X service.
- A pending/unverified member's welcome-email route returned HTTP 409 and the
  Admin UI disabled the send action. After activation/approval, the same flow
  sent the correctly branded welcome email and recorded `Welcome sent`.
- ZEC Shelf viewing and administration loaded from the Community feature
  package, with the list-number indicator absent.

Coalition-specific lifecycle checks also passed:

- An administrator created an invitation, sent its activation email, consumed
  the acceptance link, and reached the activated-account result.
- A separate applicant submitted a manual access request; an administrator
  approved it, and the active-member workspace became available.
- The active-member directory and Policy Groups were visible, and a synthetic
  resource submission reached the PGPZ team workflow.
- Invitation acceptance and manual approval synchronized the expected
  membership state into the isolated Community preview table. No production
  Community record was read or changed.

### Email rendering and tracking evidence

The newsletter flow was exercised with a one-recipient synthetic audience in
each app. Each send recorded **1 recipient, 1 sent, 0 failed, and 1 click**.
The received messages used the correct configured sender identity, Community
or Coalition brand, legal links, and their respective preview host in tracked
links. Policy-update draft messages were also delivered in both apps with the
correct subject/body, sender identity, brand, legal links, and preview-host
asset and action URLs. Welcome and invitation messages retained the appropriate
application identity. No real distribution list was selected.

## Telemetry baseline and rollback rehearsal

The aggregate 24-hour production telemetry baseline captured at
`2026-07-17T11:44:25Z` contained no `next-auth`-attributed events. Coalition
had 4 Better Auth page views from 1 user, no login event, and no magic-link
attempts; its 2 other emails were sent successfully. Community had 58 access
events from 4 users (4 logins, 54 page views), including 16 Better Auth events,
no NextAuth events, and 42 older/unattributed page views; all 4 magic-link
emails and 1 other email were sent successfully. These counts are comparison
context, not a traffic expectation or service-level denominator.

The timed rollback rehearsal began at `2026-07-17T11:39:07Z`. The disposable
apps reconnected to their original repositories with the monorepo root removed,
then deployed Coalition commit `ffffec98878729658f96ddba6624c73b316279f6`
and Community commit `d6a1d1876dbdd5f0959d43ea3c8a19ebf70334ac`.
Community job `main/1` completed at `2026-07-17T11:44:24.413Z`; Coalition job
`main/1` completed at `2026-07-17T11:44:26.661Z`. Both then passed the expanded
route/asset/API smoke and a new magic-link request by
`2026-07-17T11:46:35Z`, for a decision-to-verified duration of **7 minutes 28
seconds**. This passes the 15-minute gate with 7 minutes 32 seconds of margin.

## Go/no-go and rollback triggers

Coalition production cutover was authorized only after every pre-cutover gate
above passed. Its observation window began when job `41` succeeded at
`2026-07-17T12:41:28.186Z`. Community remains a no-go until a healthy review
after `2026-07-20 09:00` America/New_York confirms that no trigger below
occurred and closes the open administrator-only UI check.

Rollback the affected application immediately for any confirmed:

- failed deployment, wrong repository/app root, or mismatch in the recorded
  table, role, environment digest, canonical domain, brand, sender, or legal
  identity;
- canonical route, static-asset, PDF-worker, Better Auth callback/session, or
  sign-out failure;
- administrator or active-member sign-in failure, or any authorization change
  that incorrectly grants or removes protected access;
- Better Auth API/session failure rate above 1% for 15 minutes with at least 20
  attempts;
- internal magic-link send failure rate above 5% for 15 minutes, excluding
  recipient rejection or bounce;
- unrelated clients sharing a rate-limit bucket because client IPs collapse;
- profile/email change producing an identity mismatch or duplicate account;
- membership, invitation, approval, welcome-email, synchronization, branding,
  or cross-application data-boundary regression.

At the current low traffic baseline, the minimum-volume percentage thresholds
may not activate; any individually confirmed administrator, active-member,
authorization, identity, or data-boundary regression remains an immediate
rollback trigger. An application-specific regression rolls back that app. A
shared-package regression that cannot be safely reverted immediately rolls
back both apps to their original repositories.

## Rollback targets

If Coalition fails its canary, restore repository
`https://github.com/paulbrigner/pgpz-coalition`, branch `main`, and the
repository-local `amplify.yml`; leave the Amplify application-level
`buildSpec` unset, then redeploy commit
`ffffec98878729658f96ddba6624c73b316279f6`.

If Community fails after its later cutover, restore repository
`https://github.com/paulbrigner/pgpz-community`, branch `main`, and the
repository-local `amplify.yml`; leave the Amplify application-level
`buildSpec` unset, then redeploy commit
`d6a1d1876dbdd5f0959d43ea3c8a19ebf70334ac`.

`AMPLIFY_MONOREPO_APP_ROOT` must be removed when restoring either original
single-app repository. Tables, environment values, roles, SES identities, and
domains remain unchanged during rollback. Both original repositories remain
protected and deployable for at least 30 days after the second cutover.

## Production cutover log

### Coalition — deployed; observation in progress

- Preflight confirmed monorepo `main` at
  `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` and legacy Coalition `main` at
  `ffffec98878729658f96ddba6624c73b316279f6`.
- Production auto-build was disabled before changing the repository connection.
- Amplify app `d1ve1xrza71r7u` was reconnected to
  `https://github.com/paulbrigner/pgpz-sites`, branch `main`, with
  `AMPLIFY_MONOREPO_APP_ROOT=apps/coalition` and the root `amplify.yml`.
- The 18-key environment-value digest remained
  `e25dfff1a99166b1c564fc0acbdcb703ef59583105f69d8788316bcd7a80cd49`;
  the only added key is the monorepo app root. The existing app ID, platform,
  production table, IAM roles, SES/SMTP configuration, and domain association
  were unchanged.
- Explicit production job `41` ran from `2026-07-17T12:36:15.596Z` through
  `2026-07-17T12:41:28.186Z` (**5 minutes 12.590 seconds**) with `BUILD`,
  `DEPLOY`, and `VERIFY` all `SUCCEED`. Production auto-build remains disabled
  during the observation window. Re-enable it only after the healthy soak
  decision, then verify that doing so starts no unintended deployment.

Amplify persisted the job's `commitId` as the literal value `HEAD`, so this
record does not represent that field as SHA provenance. The deployed source is
instead correlated to protected `pgpz-sites/main` at
`b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea`: the branch was verified at that
SHA during and after the build, and the job log recorded cloning
`pgpz-sites` at `2026-07-17T12:36:40Z`. The build then selected the correct
`apps/coalition` environment writer and workspace, and copied the
**1,099,167-byte** `.next/server/chunks/pdf.worker.mjs` artifact.

Production verification evidence:

| Evidence | Result |
| --- | --- |
| Amplify job | `41` succeeded; `BUILD`, `DEPLOY`, and `VERIFY` succeeded in 5m12.590s |
| Source provenance | Amplify reported literal `HEAD`; protected `main`, clone timing, and build-path evidence correlate the deployment to `b36ec5e4aa5a9f05fb362c776e8c53b7ab2afeea` |
| Raw canonical HTTP smoke | 12/14 assertions passed; the two failures were SSR text-marker false negatives for home-footer legal links and sign-in controls, both resolved in the rendered browser |
| Rendered identity and navigation | Canonical home, sign-in controls, Coalition branding, legal identity/links, member directory, all 8 Groups, and profile rendered correctly |
| Better Auth and member authorization | Real production magic-link login succeeded for a controlled active non-admin account; app and Better Auth session APIs both returned HTTP 200 with the matching email, `active` membership, and `isAdmin: false` |
| Unauthenticated auth surface | Expected unauthenticated session API responses passed and the legacy NextAuth provider route returned 404 |
| Production administrator UI | pending soak item; exact candidate admin UI already passed in both isolated previews |
| Since-cutover telemetry | At `2026-07-17T12:52:18Z`: 4 access events (1 login, 3 page views), all 4 Better Auth, 0 NextAuth, 0 unattributed, 1 user; 2 magic links sent and 0 failed |
| Runtime logs | 212 events through `2026-07-17T12:52:52Z`; 0 error-, exception-, failure-, warning-, or 5xx-like matches |
| Rollback and observation | No rollback trigger observed; soak began at `2026-07-17T12:41:28.186Z` |

The active non-admin browser session reached the home workspace, member
directory, all 8 Policy Groups, and profile with the current email and legal
links. No production profile or administrator mutation was performed.

Preflight corrected the previously assumed administrator identity: production
contains exactly two active indexed administrators, while the assumed address
is not a user record. A magic link for a confirmed administrator was sent
successfully, but its mailbox was unavailable to the verification session and
no reusable authenticated browser session was available. Stored verification
tokens remained correctly hashed, so they were not recoverable from DynamoDB.
Because the exact candidate's administrator list/detail controls and
View-as-member behavior passed in both isolated previews, the missing live
administrator-only confirmation is retained as a soak item rather than
classified as a regression.

### Community — held on legacy source

Community app `d2xb9ethk5a24j` remains connected to
`paulbrigner/pgpz-community`, branch `main`, at successful job `117` and commit
`d6a1d1876dbdd5f0959d43ea3c8a19ebf70334ac`. Its environment digest, domain
associations, app root, roles, table, and deployment state remain unchanged.
Record the Coalition observation end and explicit healthy go decision after
`2026-07-20 09:00` America/New_York before any Community repository mutation.

## Preview cleanup record

Preview resources remain available during the cutover and rollback observation
window. After both production cutovers and their final validation complete,
delete the two preview Amplify apps and candidate branches, the isolated
DynamoDB tables, and the temporary compute roles. Deleting the isolated tables
also removes all synthetic application and Better Auth users, sessions,
verifications, rate-limit counters, invitations, approvals, synchronization
records, profile-change tokens, submissions, newsletter/policy drafts, email
logs, and tracking events. No production resource or record is part of this
cleanup. The current cleanup target is `2026-07-24`, conditional on the cutover
and rollback windows being complete.
